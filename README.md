# 📜 РЕГЛАМЕНТ: ПОЛУАВТОМАТИЧЕСКАЯ ПОДГОТОВКА ДАННЫХ ДЛЯ УТВЕРЖДЕНИЯ ИМЁН НА МЕМОРИАЛЬНЫХ ПЛИТАХ
**Объект:** Братское захоронение «Корпечь» (с. Фронтовое, Ленинский р-н, Республика Крым)  
**Цель:** Подать уполномоченным гос. лицам структурированные, документально подтверждённые карточки для принятия решения: *«Внести имя в список для новых плит»* или *«Отклонить»*.  
**Принцип:** В публичное пространство не попадает ни одна непроверенная запись. Система работает только как внутренний конвейер подготовки.

---

## 👥 Роли и ответственность
| Роль | Кто это | Что делает | Что НЕ делает |
|------|---------|------------|---------------|
| **Оператор-волонтёр** (Вы) | Технический куратор | Запускает сканер, контролирует импорт, готовит шаблоны, передаёт пакет на рассмотрение | Не принимает решение по именам, не публикует данные, не меняет статусы без санкции |
| **Уполномоченные лица** (Гос. учреждение) | Сотрудники архивной/мемориальной комиссии | Проверяют ссылку на ЦАМО, подтверждают/отклоняют имя, ставят визу/печать | Не работают со сканером, не правят базу, не генерируют списки |
| **Система** | WSL2-капсула + WordPress Studio | Собирает, фильтрует, формирует отчёты, хранит аудит-лог | Не принимает решений, не публикует, не открывает внешние порты |

---

## 🏗️ Архитектура (где что живёт)
```
┌──────────────────────────────────────┐
│  WSL2 (Ubuntu 24.04 + Docker)        │
│  • Chrome-headless / CDP-контейнер   │
│  • web-data-extractor (сканер)       │
│  • auth-config.json (вне git)        │
└───────────────┬──────────────────────┘
                │ Локальный IPC-мост (STDIO/Unix-socket или localhost:9999)
                ▼
┌──────────────────────────────────────┐
│  Windows 11                          │
│  • WordPress Studio (studio-setup-   │
│    x64-v1.8.0.exe)                   │
│  • CPT: tsamo_card                   │
│  • Конфигуратор шаблонов (визуальный)│
└──────────────────────────────────────┘
```
> 🔒 **Безопасность связи**: Сканер не выходит в сеть кроме целевых архивных порталов. Связь с WP идёт только через `localhost` или локальный сокет. Внешние порты закрыты.

---

## 🔄 Пошаговый регламент работы

### 🔹 Этап 1. Сбор и фильтрация (Оператор)
1. В WSL2 запускается сканер-контейнер.
2. Оператор вручную проходит авторизацию/капчу на портале (pamyat-naroda.ru / obd-memorial.ru).
3. Сканер в полуавтоматическом режиме извлекает карточки, где в поле захоронения/перезахоронения указано: `Корпечь`, `Фронтовое`, `выс. 66,3`, `Кой-Асан`.
4. Данные передаются в WordPress через локальный транспорт. Система создаёт записи со статусом `draft`.

### 🔹 Этап 2. Внутренняя обработка (Система)
1. WP автоматически:
   - Проверяет обязательные поля (ФИО, дата смерти, номер дела, источник).
   - Ищет дубли (по ФИО + дата + номер дела).
   - Помечает записи: `ready_for_template` или `needs_manual_check`.
2. Оператор видит список в админке, при необходимости добавляет заметки.

### 🔹 Этап 3. Формирование пакета для гос. органов (Оператор + Шаблоны)
1. В WP открывается раздел **«Шаблоны уведомлений»**.
2. Оператор визуально настраивает два блока (без правки кода):
   - **Email-шаблон** (для отправки уполномоченным)
   - **Веб-зеркало** (страница проверки по уникальной ссылке)
3. Система генерирует пакет: 1 письмо = 1 воин. Каждое письмо содержит уникальную ссылку-зеркало вида `/verify/ID_XXXX`.

### 🔹 Этап 4. Проверка и решение (Уполномоченные лица)
1. Гос. сотрудник получает письмо.
2. Переходит по ссылке-зеркалу → видит краткие данные + кнопку `Перейти к источнику ЦАМО`.
3. Сверяет документ, принимает решение в системе:
   - ✅ `Утвердить для плит`
   - ❌ `Отклонить` (с указанием причины)
   - 📝 `Запросить доп. проверку`
4. Решение фиксируется с датой, IP, логином. Статус записи обновляется.

### 🔹 Этап 5. Экспорт и передача (Оператор)
1. Фильтр: `status = approved_for_plate`.
2. Экспорт в `memorial_plates_final.csv` (формат для гравировальной мастерской).
3. Пакет передаётся по утверждённому каналу связи гос. учреждения.
4. Все остальные статусы остаются во внутреннем контуре. Публикация запрещена.

---

## 🎨 Как реализуется «визуальная подготовка шаблонов»

Вам не нужно писать PHP-шаблоны. В WordPress Studio создаётся простой конфигуратор:

### 1. Веб-зеркало (страница проверки)
- Создаётся файл `mu-plugins/verify-mirror-config.php`.
- В админке WP появляется меню: `Настройки → Шаблон веб-зеркала`.
- Поле: HTML-разметка с переменными:
  ```html
  <h2>{fio}</h2>
  <p>Звание: {rank} | Дата гибели: {death_date}</p>
  <p>Источник: <a href="{source_link}" target="_blank">Открыть документ ЦАМО</a></p>
  <form action="/verify/{id}" method="POST">
    <button name="action" value="approve">✅ Утвердить для плит</button>
    <button name="action" value="reject">❌ Отклонить</button>
  </form>
  ```
- Оператор меняет текст, цвета, расположение кнопок прямо в поле. Сохраняет → шаблон применяется мгновенно.

### 2. Email-уведомление
- Аналогичное поле в `Настройки → Шаблон письма`.
- Поддерживает переменные: `{fio}`, `{rank}`, `{unit}`, `{source_link}`, `{mirror_link}`, `{operator_name}`.
- Предпросмотр доступен по кнопке `Тестовый просмотр` (рендерит в модальном окне).

### 3. Хранение
- Шаблоны сохраняются в БД (опции WP) или в локальном JSON-файле `wp-content/config/templates.json`.
- Версионируются отдельно от кода сканера.

---

## 🔒 Правила безопасности и этики
1. **Ноль публичности.** Веб-зеркала не индексируются, доступны только по прямой ссылке. Нет поиска, нет каталога.
2. **Цепочка доказательств.** Каждое имя в итоговом CSV привязано к `tsamo_case_number` и `source_link`. Нет ссылки = нет в списке.
3. **Аудит.** Все действия логируются: кто запустил сканер, когда импортировано, кто утвердил, дата решения.
4. **Изоляция.** Сканер в WSL2/Docker. WP на Win11. Связь только локально. `auth-config.json` и токены вне репозитория.
5. **Этика.** Система не генерирует «возможно» или «предположительно». Только факт из документа + решение уполномоченного лица.

---

## 📌 Чек-лист запуска (минимальный рабочий цикл)
- [ ] WSL2 + Docker: контейнер с Chrome CDP + `web-data-extractor` настроен
- [ ] Windows 11: WordPress Studio установлен, сайт `yc-archives-lab` запущен
- [ ] CPT `tsamo_card` зарегистрирован, поля ACF сопоставлены с `data_csv2.csv`
- [ ] Локальный мост (STDIO/IPC) между WSL2 и WP проверен (тестовая отправка 1 записи)
- [ ] В WP созданы конфигураторы шаблонов (веб-зеркало + email)
- [ ] Протестирован полный цикл: сканер → WP → шаблон → ссылка → кнопка решения → экспорт CSV

---


-------
# Формулирую

---

## Моё понимание того, что  делаю

Я не строю базу данных.

Я восстанавливаю **связи**, которые война разорвала.

---

### Цифры, которые говорят сами за себя

3310 человек.

Из них **59 известных**.

**3251 неизвестных**.

Три тысячи двести пятьдесят один сын, брат, отец, чьё имя не высечено на камне.

Они погибли декабрь 1941 - июнь 1942  года.  Последнее пере-захоронение датировано апрелем 1944 года.

Три с половиной года умирать. Три с половиной года хоронить.

---

### Что такое братское кладбище

Это не просто яма, где лежат тела.

Это место, где **мать потеряла не одного сына, а многих**.

У них одна мать. Имя ей — Родина.

Я не делю их по крови, по званию, по возрасту. Они все — **ЕЁ дети**.

---

### Рассекреченные документы

В 2007 году Министр обороны РФ подписал приказ №181.

Архивы Красной Армии и Военно-Морского Флота перестали быть тайной.

Два года спустя, в 2009-м, приказ уточнили и дополнили.

С этого момента **документы заговорили**.

Но они заговорили на языке, который понимают только те, кто умеет читать между строк: описи, фонды, дела, страницы, пометки на полях.

---

### Моя работа

Я беру эти рассекреченные документы и **превращаю** в данные.

Данные, которые можно:

- Искать
- Связывать
- Проверять
- Экспортировать в «Мост поколений» — российский аналог MyHeritage

Чтобы алгоритмы сами находили тех, кто ждал.

---

### Те, кто ждал

В документах есть **два поля**, которые важнее всего:

- Кого боец указал, чтобы сообщили, если он не вернётся
- Адрес, куда должна прийти похоронка

Это не просто строчки.

Это **последний крик живого**: "Мама, я здесь. Если не дойду — сообщите ей".

---

### Волонтёры

Я приглашаю волонтёров не вводить данные.

Я приглашаю их **восстанавливать связи**.

Каждый заполненный адрес, каждое имя того, кто ждал, — это **нить**, которая может привести к живым потомкам.

А «Мост поколений» — российский аналог MyHeritage пусть стучится в их почту.

---

### Гит

Каждый коммит в Git — это **клеймо**.

"Эта информация проверена. Этот человек лежит здесь. Эта мать ждала именно его."

Git хранит время. Git хранит автора. Git хранит **доказательство**.

---

### Камень

В Корпечи — братское захоронение.

На камне сейчас, возможно, нет имён.

Но будут.

Потому что 3251 неизвестный — это не число.

Это **люди**, у каждого из которых была мать.

И я хочу, чтобы **её имя** оказалось рядом с его именем.

На граните. В интернете. В памяти.

---

### Патетика

Да, я не боюсь пафоса.

Они гибли, чтобы мы могли говорить.

Рассекреченные документы — это их **последнее письмо домой**.

А я — тот, кто доставит это письмо.

---

> Этот проект — не просто техническая задача. Это возможность вернуть имена тем, кто лежит в братской могиле.\
> Каждая строка кода, каждая настройка безопасности — часть уважения к памяти.\
> Техника должна быть такой же бережной, как и смысл.

\
<https://obd-memorial.ru/html/info.htm?id=33125225&p=2>