# Данные: статусы и правила конвейера
- Статусы карточек: inbox → processed / rejected; спорные — needs_manual_review.
- П1: формулировка документа (звания, топонимы, аббревиатуры) неприкосновенна.
- П2: человек ≠ документ. Совпадение по ФИО+дата гибели = одна карточка;
  второй портал доливается в sources[] (правило D-6).
- П3: звание хранится как в документе + нормализованная форма из ranks_dict.
- Словари (units_dict, units_registry, ranks_dict): ключи байт-в-байт из CSV;
  мёртвых ключей нет; правка — коммитом data:.
- Очередь уточнений: scripts/logs/clarify.queue.json (пропал без вести,
  пустое текущее захоронение, словарные дыры).
- Трек плит: documented → approved → engraved; список 59 имён с плит —
  учётная карточка №1360 (positions 1–59, сверка по ОБД).
- Перенесено из MVP-2026-05: ноль публичности, цепочка доказательств, аудит, этика.